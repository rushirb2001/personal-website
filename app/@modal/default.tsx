// The modal parallel-route slot renders nothing unless an intercepting route
// matches (i.e. a project case study opened via in-app navigation).
export default function ModalDefault() {
  return null
}
