description = "Runs the local development server for the current project."
prompt = """
Your goal is to start the local development server for the current project.

1. Inspect `package.json` to find the correct development command (e.g., `npm run dev`, `start`, etc.).
2. Run the command in the background.
3. Notify the user that the dev server has been started and provide the local URL.
"""
